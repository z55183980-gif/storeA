# Goal progress

## 已完成

- 项目根目录锁定为 `E:\storeA`。
- 数据库按用户最新要求改为 SQLite，文件为 `database/store.sqlite`。
- PHP 已启用 `pdo_sqlite` 和 `sqlite3`。
- 已建立核心表、索引、外键、种子商品和初始管理员。
- 商品目录采用动态数量设计，10个仅为当前经营规模，不作为系统硬限制。
- 已建立商品、注册登录、游客/用户订单、游客客服会话和消息 API。
- 已建立用户端首页和第一版管理工作台。
- 已完成首轮 API 冒烟测试：商品、注册、游客客服、消息和下单均成功。
- 新商城用户端运行在 `http://127.0.0.1:5173/`，管理端运行在 `http://127.0.0.1:5174/`，8080 仅提供后端 API。

## 已追加完成

- Vue 3 + Vite 用户端与管理端工程已建立并完成生产构建。
- 管理员签名令牌认证、商品 CRUD、订单状态管理和客服回复 API 已完成。
- 用户端购物车、注册登录、下单和游客客服窗口已完成。
- Nginx + PHP-CGI + SQLite 后端已启动并通过验证；前端通过 Vite 5173/5174 提供服务。
- 已移除商品数量触发器，商品数量不再被固定上限限制。
- build/start/stop/test 脚本和完整 README 已补齐。

## 最终验收完成

- `scripts\build.ps1` 构建用户端和管理端成功。
- `scripts\test.ps1` 通过健康检查、商品数量、前后台入口、管理员认证、游客客服和未授权订单访问测试。
- 额外验证用户注册下单、非法订单状态拒绝、管理员客服回复闭环和第11个商品拒绝。
- 最终数据库已重置为3个种子商品，运行端口为8080、9000、6379。
